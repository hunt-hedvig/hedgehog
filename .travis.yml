language: node_js

node_js:
  - '10'

cache:
  directories:
    - node_modules

install:
  - npm i -g yarn@1.22.0
  - yarn

script:
  - yarn lint
  - yarn typecheck
  - yarn test
  - yarn build

notifications:
  email: false
