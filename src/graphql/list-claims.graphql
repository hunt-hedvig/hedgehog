query ListClaims($options: ListClaimsOptions!) {
  listClaims(options: $options) {
    claims {
      id
      member {
        memberId
        firstName
        lastName
      }
      registrationDate
      claimType
      state
      reserves
    }
    page
    totalPages
  }
}
