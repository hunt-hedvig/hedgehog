// @ts-nocheck
import React from 'react'
import { TicketHistory } from '../../components/tickets/ticket/history/ticket-history'

const TicketHistoryPage = (props) => {
  return <TicketHistory id={props.ticketId} />
}

export default TicketHistoryPage
