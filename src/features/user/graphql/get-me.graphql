query GetMe($tracking: UserTrackingData) {
  me(tracking: $tracking) {
    scopes
    role
    user {
      id
      email
      fullName
      phoneNumber
    }
    settings {
      key
      value
    }
  }
}
