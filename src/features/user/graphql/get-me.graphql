query GetMe {
  me {
    scopes
    role
    user {
      id
      email
      fullName
      signature
      phoneNumber
      role
      notifications {
        id
        message
        url
        createdAt
        read
        from {
          id
          signature
          fullName
        }
      }
    }
    settings {
      key
      value
    }
  }
}
