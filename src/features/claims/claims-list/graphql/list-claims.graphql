query ListClaims($options: ListClaimsOptions!) {
  listClaims(options: $options) {
    claims {
      id
      member {
        memberId
        firstName
        lastName
      }
      payments {
        amount
      }
      registrationDate
      claimType
      outcome
      state
      reserves
    }
    page
    totalPages
  }
}
