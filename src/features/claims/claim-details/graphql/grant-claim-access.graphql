mutation GrantClaimAccess($claimId: ID!, $email: String!) {
  grantClaimAccess(claimId: $claimId, email: $email) {
    id
    claim {
      id
    }
    grantedBy {
      id
      email
      fullName
    }
    grantedTo {
      id
      email
      fullName
    }
  }
}
