mutation RestrictResourceAccess($resourceId: ID!) {
  restrictResourceAccess(resourceId: $resourceId) {
    resourceId
    usersGranted {
      id
      email
      fullName
      role
    }
    usersRestricted {
      id
      email
      fullName
      role
    }
    rolesGranted
    rolesRestricted
  }
}
