mutation RestrictClaimAccess($claimId: ID!) {
  restrictClaimAccess(claimId: $claimId) {
    id
    claim {
      id
    }
    grantedBy {
      id
      email
      fullName
    }
    grantedTo {
      id
      email
      fullName
    }
  }
}
