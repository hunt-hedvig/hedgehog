query SOSMemberLookup($ssn: String!) {
  SOSMemberLookup(ssn: $ssn) {
    memberId
    firstName
    lastName
    email
    phoneNumber
    market
    contracts {
      contractId
      typeOfContract
    }
  }
}
