query SOSMemberLookup($ssn: String!) {
  SOSMemberLookup(ssn: $ssn) {
    memberId
    firstName
    lastName
    email
    phoneNumber
    market
    contracts {
      contractId
      typeOfContract
      masterInception
      terminationDate
      numberCoInsured
      address {
        street
        postalCode
        city
      }
    }
  }
}
