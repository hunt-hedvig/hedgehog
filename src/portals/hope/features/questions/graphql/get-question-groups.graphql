query GetQuestionsGroups {
  questionGroups {
    id
    memberId
    memberId
    firstName
    lastName
    market
    pickedLocale
    questions {
      id
      messageJsonString
      timestamp
    }
  }
}
