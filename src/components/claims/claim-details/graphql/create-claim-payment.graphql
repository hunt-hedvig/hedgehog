mutation CreateClaimPayment($id: ID!, $payment: ClaimPaymentInput!) {
  createClaimPayment(id: $id, payment: $payment) {
    payments {
      id
      amount
      deductible
      exGratia
      note
      timestamp
      type
      status
    }
  }
}
